// TODO: debouncing 유틸화
export default searchKeywordHandler = (keyword) => {
  // let timer = null

  // if (timer) {
  //   clearTimeout(timer)
  // }
  // timer = setTimeout(() => {
  //   this.searchHandler(keyword)
  //   this.render()
  // }, 300)

}
